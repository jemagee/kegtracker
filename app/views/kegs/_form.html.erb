<% if keg.errors.any? %>
  <%= render 'layouts/errors', object: keg %>
<% end %>
<%= form_for(keg) do |f| %>

  <%= f.label :serial_number %>
  <%= f.text_field :serial_number %>

  <%= f.submit %>

<% end %>