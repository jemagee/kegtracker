<%= @batch.flavor.name %>
<%= @batch.lot %>
<div id="gallons">
	<%= @batch.gallons %>
</div>

Best By Date: <%= @batch.best_by %>
<%=link_to "Edit Batch", edit_batch_path(@batch) %>
<%= link_to "Delete Batch", batch_path(@batch), method: :delete, data: {confirm: "Are you sure you want to delete this batch?"} %>